import { createTheme } from '@mui/material/styles'

// The full documentation about theming should be read on Material UI docs: https://mui.com/pt/customization/theming/
const Theme = createTheme({})
export default Theme
